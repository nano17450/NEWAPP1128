name: elevate_app_v2
description: Una sencilla app de empresa en Flutter
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^2.32.0
  firebase_auth: ^4.20.0
  cloud_firestore: ^4.17.0
  js: ^0.6.7
  file_picker: ^6.1.1
  firebase_storage: ^11.7.0
  country_picker: ^2.0.21
  csv: ^5.0.2
  universal_html: ^2.2.4
  intl: ^0.19.0
  geolocator: ^10.1.0
  shared_preferences: ^2.2.2
  permission_handler: ^11.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/logo.png